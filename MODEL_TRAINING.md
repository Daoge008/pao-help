# 实物跑胡子卡牌检测模型训练与端侧部署指南

本文档提供针对**线下实物跑胡子（字牌）**的目标检测模型（YOLOv8n / YOLO11n）全流程训练指南，包含数据合成、标注格式、训练配置与端侧 TFLite 量化导出。

---

## 1. 卡牌类别定义 (20 类别)

跑胡子模型需识别 20 种卡牌类别。类别索引与标签对应表如下（对应 `assets/models/labelmap.txt`）：

| 索引 | 标签代码 | 卡牌汉字 | 类别属性 |
| :---: | :---: | :---: | :---: |
| 0 | `s1` | 一 | 小字 (黑) |
| 1 | `s2` | 二 | 小字 (红) |
| 2 | `s3` | 三 | 小字 (黑) |
| 3 | `s4` | 四 | 小字 (黑) |
| 4 | `s5` | 五 | 小字 (黑) |
| 5 | `s6` | 六 | 小字 (黑) |
| 6 | `s7` | 七 | 小字 (红) |
| 7 | `s8` | 八 | 小字 (黑) |
| 8 | `s9` | 九 | 小字 (黑) |
| 9 | `s10` | 十 | 小字 (红) |
| 10 | `b1` | 壹 | 大字 (黑) |
| 11 | `b2` | 贰 | 大字 (红) |
| 12 | `b3` | 叁 | 大字 (黑) |
| 13 | `b4` | 肆 | 大字 (黑) |
| 14 | `b5` | 伍 | 大字 (黑) |
| 15 | `b6` | 陆 | 大字 (黑) |
| 16 | `b7` | 柒 | 大字 (红) |
| 17 | `b8` | 捌 | 大字 (黑) |
| 18 | `b9` | 玖 | 大字 (黑) |
| 19 | `b10` | 拾 | 大字 (红) |

---

## 2. 实物数据集构建方案

由于实物跑胡子牌面字迹固定且形状为狭长条形（长宽比通常在 3.5:1 ~ 4:1 之间），推荐采用**合成数据增强为主 + 实景真机抽样验证**的高效构建方案。

### 自动化合成脚本思路 (Python + PIL/OpenCV)
1. **抠取 20 张清晰卡牌单图**：保存为无背景透明 PNG 图章。
2. **随机背景库**：收集常见牌桌背景（绿色麻将桌布、实木纹理、反光亚克力桌面等）。
3. **空间排列模拟**：
   * **手牌模式**：在画面下方（$Y \in [0.65, 0.95]$）按角度扇形排布（轻微叠放、遮挡 15%~30%）；
   * **桌面弃牌模式**：在画面中央和上方随机散落排列。
4. **图像增强增强泛化**：
   * 随机光照强弱变化（模拟室内白炽灯/暗光环境）；
   * 透视形变（Perspective Transform，模拟手机俯视与斜射角度）；
   * 高斯模糊、椒盐噪声与局部阴影。

---

## 3. YOLO 模型训练步骤

推荐使用官方 Ultralytics 框架训练 `yolov8n` 或 `yolo11n`（Nano 级轻量模型，推理耗时低至 15~25ms）：

### 数据集配置文件 `pao_cards.yaml`
```yaml
path: ./dataset
train: images/train
val: images/val

names:
  0: s1
  1: s2
  2: s3
  3: s4
  4: s5
  5: s6
  6: s7
  7: s8
  8: s9
  9: s10
  10: b1
  11: b2
  12: b3
  13: b4
  14: b5
  15: b6
  16: b7
  17: b8
  18: b9
  19: b10
```

### 训练指令
```bash
# 安装 ultralytics
pip install ultralytics

# 启动训练 (输入分辨率 640，训练 100 轮)
yolo detect train \
  data=pao_cards.yaml \
  model=yolo11n.pt \
  epochs=100 \
  imgsz=640 \
  batch=32 \
  device=0
```

---

## 4. 导出为移动端 TFLite 模型

训练收敛后（mAP@0.5 达到 0.95 以上），导出为针对移动端优化的 TensorFlow Lite 模型：

```bash
# 导出 FP16 精度 TFLite 模型
yolo export model=runs/detect/train/weights/best.pt format=tflite imgsz=640 half=true
```

导出成功后会生成 `best_float16.tflite`。
将其重命名并拷贝到 Flutter 项目中：
```bash
cp best_float16.tflite pao-help/assets/models/pao_yolov8n.tflite
```

---

## 5. 端侧推理优化技巧

1. **分辨率选择**：跑胡子字迹大、特征清晰，输入分辨率推荐设置为 `480x480` 或 `640x640`，兼顾远端小字清晰度与运行帧率。
2. **多线程加速**：在 Android 设备上使用 TFLite NDK 运行时，设置 CPU 线程数为 4，或启用 NNAPI / GPU Delegate 加速。
3. **时序平滑去抖**：本项目内置的 `SpatialTracker` 已经实现了多帧交并比匹配与去噪过滤，即使模型在单帧出现瞬时误检或漏检，也不会造成 UI 建议频繁闪烁。
