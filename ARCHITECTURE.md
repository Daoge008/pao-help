# 跑胡子助手（Pao-Help）系统架构与设计说明

本文档详细描述系统的分层架构、数据流向、计算机视觉流水线、规则引擎与 AI 决策推荐算法。

---

## 1. 系统整体架构

系统采用清晰的响应式四层架构：

```
┌────────────────────────────────────────────────────────┐
│                   UI 展示与交互层 (Flutter)              │
│  - CameraScannerScreen: 连续视频流渲染与触摸交互         │
│  - CardOverlayPainter: 卡牌归一化坐标边框与类别绘制      │
│  - DecisionHUD: 核心动作建议、打分与理由面板             │
│  - RuleSettingsScreen: 参数化规则微调与预设切换          │
└───────────────────────────▲────────────────────────────┘
                            │ Provider 状态驱动刷新
┌───────────────────────────┴────────────────────────────┐
│               实时视觉流水线 (VisionPipeline)          │
│  - 帧率智能节流（每秒 6~8 帧防发热控制）               │
│  - YoloDetector: TFLite 前向推理与 NMS 去重              │
│  - SpatialTracker: 多帧空间坐标关联与时序去抖动        │
│  - 视野区域动态分割（手牌区 vs 桌面弃牌区 vs 焦点牌区）│
└───────────────────────────▲────────────────────────────┘
                            │ 稳定状态输出
┌───────────────────────────┴────────────────────────────┐
│                 牌局状态与决策引擎 (Engine)             │
│  - PaoGameState: 记录手牌、明门子、桌面弃牌、出牌权     │
│  - DecisionEngine: 评估【胡/提/跑/偎/碰/吃/出牌/过】    │
│  - Shanten & Expectation: 向听数测算与胡息期望打分      │
│  - Safety Evaluator: 桌面未现牌概率推算与安全牌过滤    │
└───────────────────────────▲────────────────────────────┘
                            │ 规则约束与分值查询
┌───────────────────────────┴────────────────────────────┐
│                  规则与名堂计算引擎 (Rules)            │
│  - RuleConfig: 默认湖南常德跑胡子 (15胡起) 参数配置      │
│  - HuXiCalculator: 坎/碰/偎/提/跑/一二三/二七十 胡息矩阵│
│  - HuChecker: 递归回溯与分支剪枝的合法胡牌拆解器       │
│  - MingTangEvaluator: 红胡/点胡/黑胡/十三红/碰碰胡结算 │
└────────────────────────────────────────────────────────┘
```

---

## 2. 连续视频流与空间去抖算法 (Spatial Tracking)

实物对局中，手部微动、卡牌轻微位移、光线变化容易导致单帧检测跳动。系统设计了专门的 `SpatialTracker`：

1. **空间欧氏距离与类别双重匹配**：
   对于当前帧新识别的卡牌 $C_{new}(x, y)$ 与上一帧追踪池 $T_{prev}(x', y')$：
   $$\text{Dist} = \sqrt{(x - x')^2 + (y - y')^2}$$
   当同名卡牌距离小于阈值（默认 $\le 0.08$ 归一化单位）时判定为同一张物理卡牌，采用指数加权移动平均更新坐标：
   $$x_{smooth} = 0.6 \cdot x_{prev} + 0.4 \cdot x_{raw}$$
2. **时序平滑过滤机制**：
   * **确认阈值（Hit Streak）**：只有连续至少 3 帧稳定出现的卡牌，才会加入手牌或桌面状态；
   * **移除延迟（Miss Streak）**：连续丢失超过 4 帧才从状态中剔除，有效抵抗手部挥动瞬间带来的临时遮挡。
3. **空间区域自适应判定**：
   * $Y \ge 0.62$：识别为持牌人手牌区；
   * $0.38 \le Y < 0.62$：识别为当前翻出或刚打出的焦点牌（触发碰/吃/偎/胡判定）；
   * $Y < 0.38$：识别为桌面公共弃牌区（计入防守安全性统计）。

---

## 3. 跑胡子拆解与胡牌判定算法 (HuChecker)

跑胡子的成胡判断采用**多叉树回溯递归算法**，核心逻辑如下：

1. 统计当前手牌的所有牌张频次哈希表；
2. 按照以下门子优先级依次尝试递归拆解：
   * **提/坎**：3 张相同或 4 张相同；
   * **将牌**：至多允许消耗 1 对（2张相同）作为半块；
   * **二七十**：2-7-10 或 贰-柒-拾；
   * **一二三**：1-2-3 或 壹-贰-叁；
   * **顺子**：连续三张连号；
   * **绞牌**：同点数的两小一大或两大小一。
3. 当所有手牌成功拆尽时，计算拆出组合的总基础胡息；
4. 若基础胡息 $\ge \text{minHuXi}$（常德默认为 15），则该组合为合法胡牌，取所有合法组合中胡息最高且番数最大的最优解输出。

---

## 4. 动作推荐与打分模型 (DecisionEngine)

当牌局出现新牌或进入我方出牌阶段，AI 引擎会对候选动作执行加权评分（0 ~ 100 分）：

### A. 即时进牌阶段
* **胡牌（Score = 100）**：满足 15 胡及以上起胡条件，无条件推荐立即胡牌。
* **提/跑牌（Score = 94 ~ 98）**：增加大额胡息且通常为规则强制动作，优先执行。
* **偎牌（Score = 90）**：自摸暗亮，既保留门子隐蔽性又增加胡息。
* **碰牌（Score = 80）**：锁定对子门子，根据大字（+3胡）与小字（+1胡）动态微调。
* **吃牌（Score = 70 + $\Delta \text{胡息}$）**：若能组成一二三或二七十，得分大幅增加；否则作为消化孤张的普通操作。
* **过牌（Score = 50）**：当进牌收益过低或破坏手牌完整性时推荐放过。

### B. 我方出牌阶段
系统评估手牌中每张可能打出的牌，综合以下维度加权排序：
1. **结构破坏度**：拆坎扣 40 分，拆对扣 20 分；
2. **孤张优先度**：无任何前后邻牌且数量为 1 的纯孤牌加 25 分；
3. **红字保留策略**：常德跑胡子红胡加翻，若全手红字较多（$\ge 8$ 张），保留红字牌；
4. **防守安全性**：桌面已现 3 张以上的牌属于绝张熟牌，加 15 分优先打出避险。
