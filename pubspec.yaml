name: pao_help
description: "线下实物跑胡子（常德跑胡子）实时视觉辅助与决策建议助手"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.11.0+2
  tflite_flutter: ^0.12.1
  provider: ^6.1.2
  shared_preferences: ^2.2.3
  image: ^4.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/models/
    - assets/models/labelmap.txt
